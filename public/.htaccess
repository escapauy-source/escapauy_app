RewriteEngine On

# 1) No tocar si es archivo o carpeta existente (incluye /assets/*.js, .css, imagenes, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2) Fallback de SPA: todo lo demas va a index.html
RewriteRule . /index.html [L]
